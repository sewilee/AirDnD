import React from 'react';
import NavBarContainer from '../nav_bar/nav_bar_container';

const Splash = (props) => {
    return (
        <div className="splash-page">
            < NavBarContainer page="splash"/>
        </div>
    )
}

export default Splash;